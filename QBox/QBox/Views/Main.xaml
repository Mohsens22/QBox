<Page
    x:Class="QBox.Views.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QBox.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:VM="using:QBox.ViewModels"
    xmlns:controls="using:QBox.Controls"
    mc:Ignorable="d">

    <Page.DataContext>
        <VM:SwipePanelViewModel />
    </Page.DataContext>
    <StackPanel Background="Transparent">
        <StackPanel Margin="30,0,0,0" Orientation="Horizontal" >
            <Ellipse Margin="0,0,0,0" VerticalAlignment="Top"  HorizontalAlignment="Center" Height="100"  Width="100" >
            <Ellipse.Fill>
                <ImageBrush Stretch="UniformToFill" x:Name="avatar" ImageSource="{Binding Picture}" />
            </Ellipse.Fill>
            </Ellipse>
            <TextBlock Text="{Binding Greet}"  FontWeight="SemiBold" VerticalAlignment="Center"
             TextAlignment="Center" x:Name="news" FontSize="30"  />

        </StackPanel>

        <!--<controls:GraphControl  Header="Your performance" Done="3" Pending="2" Height="300" Margin="30,5" Items="{x:Bind VM.items}" />-->
        
    </StackPanel>
</Page>
